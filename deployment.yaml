apiVersion: apps/v1
kind: Deployment
metadata:
  name: brain-app-deployment
spec:
  replicas: 2
  selector:
    matchLabels:
      app: brain-app
  template:
    metadata:
      labels:
        app: brain-app
    spec:
      containers:
      - name: brain-app
        image: 828862865552.dkr.ecr.ap-south-1.amazonaws.com/brain-app:latest
        ports:
        - containerPort: 80
